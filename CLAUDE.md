# 作業ループ仕様

メインエージェントはオーケストレーションに専念し、`TASK.md` の指示の実行（PRレビューの推論を含む）は必ずサブエージェントに委譲する。これはメインエージェント自身のコンテキスト消費を抑えるだけでなく、Anthropic/OpenAIの従量課金APIを使わず、Claude Codeのサブスクリプションの範囲内でレビューを完結させるための設計でもある。

## ループルール

1. LangGraph を起動する
   ```bash
   venv/bin/python langgraph_orchestrator.py
   ```
2. 標準出力の1行目が `DONE` であれば → 以下のコマンドを実行してセッションを終了する（コミット・質問・確認は不要）
   ```bash
   tmux kill-session -t $(tmux display-message -p '#S')
   ```
3. 標準出力の1行目が `ABORT` であれば → ループを中断する（後述の「ABORT（異常終了）」を参照。`tmux kill-session` は実行しない）
4. どちらでもなければ、その1行目を `EXECUTION_DIRECTIVE`（後述）として解釈し、それに従ってサブエージェントをスポーンし `TASK.md` の指示を実行させる
5. サブエージェント完了後、1 に戻る

## 終了条件

- 正常終了: LangGraph起動時の標準出力の1行目が `DONE` であること
- 異常終了: LangGraph起動時の標準出力の1行目が `ABORT` であること（無限ループ防止。詳細は後述の「ABORT（異常終了）」を参照）

## EXECUTION_DIRECTIVE

`langgraph_orchestrator.py` 起動時、作業が残っている場合は標準出力の1行目に次の形式のJSONを出力する（`TASK.md` の内容は標準出力には出さない）:

```json
{"run": "claude", "agent": "general-purpose", "context": "new"}
```

- `run`: 実行エージェントの種類
  - `"claude"` → Claude Codeのサブエージェントとして起動する（現時点で唯一の値。将来 `"codex"` 等が追加されたら該当のCLIエージェントを起動する）
- `agent`: スポーンするサブエージェントの種別（標準の `general-purpose` 等、または `.claude/agents/` に定義したカスタムサブエージェント名）。`subagent_type: <agent>` として指定する
- `context`: コンテキストの管理方法
  - `"new"` → 新規コンテキストでサブエージェントを起動する（タスクがメインエージェントの会話履歴に依存しない場合）
  - `"fork"` → メインエージェントのコンテキストをフォークしてサブエージェントを起動する（タスクが直前までの文脈を必要とする場合。利用できない場合は `"new"` にフォールバックする）

`EXECUTION_DIRECTIVE` が取得できない場合は `{"run": "claude", "agent": "general-purpose", "context": "new"}` をデフォルトとする。

サブエージェントには「`TASK.md` を読み、指示に従って作業し、完了したら結果を報告せよ」と指示する（メインエージェント自身は `TASK.md` の内容を読み込まない）。

## ABORT（異常終了）

以下のいずれかに達すると、`langgraph_orchestrator.py` はループを継続せず標準出力の1行目に `ABORT` を出力する（サブエージェント自身のトークン使用量は外部から観測できないため、料金やトークン数ではなく回数で無限ループを検知する）:

- **redo回数の上限**: check フェーズで redo（差戻し）が累計5回発生した
- **反復回数の上限**: ループ全体（LangGraph起動）の累計回数が30回に達した
  - redoが発生しないパターン（例: サブエージェントが指示通りの完了条件ファイルを書き出せず同じフェーズが再ディスパッチされ続ける）を検知するための保険

2行目には、どちらの場合も次の形式のJSONを出力する:

```json
{"step": "観点 1/3: シークレット・APIキーのハードコード のレビュー検証（check）フェーズ", "reason": "redo（差戻し）が累計5回発生したため、無限ループ防止のため中断しました。"}
```

- `step`: どの手順で中断したか
- `reason`: なぜ中断したか

`ABORT` を検知したら、メインエージェントはループを中断し（LangGraphの再起動・サブエージェントのスポーンは行わない）、この2行目のJSONの内容をもとに「どの手順で中断したか」「なぜ中断したか」をユーザーに報告する。
