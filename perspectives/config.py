"""
PR レビュー観点の定義。

第1段階: AI 検出精度 ◎ の項目（id 0-2）。
第2段階: ◎ の誤検知が少ないことを確認できたため、○ 中精度の項目（id 3-12）を追加。
"""

PERSPECTIVES = [
    {
        "id": 0,
        "name": "シークレット・APIキーのハードコード",
        "category": "セキュリティ & プライバシー",
        "severity": "高",
        "review_prompt": """\
あなたはセキュリティレビューを担当するエージェントです。
以下のPR内容を確認し、ソースコード内にAPIキー・パスワード・トークン・データベース接続文字列などの
機密情報がハードコードされている箇所がないかチェックしてください。

問題とみなすもの:
- const API_KEY = "sk-xxxx" のような文字列リテラルの機密情報
- データベース接続文字列に認証情報が含まれている (postgres://user:pass@host/db)
- JWTシークレットのハードコード
- ソースコードに直書きされた秘密鍵・証明書

問題とみなさないもの:
- os.environ["API_KEY"] のような環境変数からの読み取り
- プレースホルダー ("YOUR_API_KEY_HERE", "REPLACE_ME")
- テスト用のダミー値であることが明示されているもの
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するシークレット・APIキーのハードコードに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: PRに実際にハードコードされた機密情報があるのに指摘していない
2. 誤検知: 環境変数・プレースホルダー・ダミー値を誤ってハードコードと判断している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 1,
        "name": "機密情報のログ出力",
        "category": "セキュリティ & プライバシー",
        "severity": "高",
        "review_prompt": """\
あなたはセキュリティレビューを担当するエージェントです。
以下のPR内容を確認し、機密情報がログに出力されている箇所がないかチェックしてください。

問題とみなすもの:
- パスワード・トークン・APIキーをログ出力している
- ユーザーの個人情報（メールアドレス・電話番号・住所など）をそのままログに記録
- 認証ヘッダーの内容をログに出力 (Authorization: Bearer xxxx)
- エラーメッセージに機密情報をそのまま含めている

問題とみなさないもの:
- ユーザーIDやリクエストIDなどの識別子のログ出力
- マスク処理済みの情報 ("****" など)
- ステータスコードやエラー種別のみのログ
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対する機密情報のログ出力に関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際に機密情報がログ出力されているのに指摘していない
2. 誤検知: IDやステータスなど機密でない情報を誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 2,
        "name": "デッドコード・未使用変数",
        "category": "保守性 & コード品質",
        "severity": "低",
        "review_prompt": """\
あなたはコード品質レビューを担当するエージェントです。
以下のPR内容を確認し、デッドコードや未使用変数がないかチェックしてください。

問題とみなすもの:
- return文・throw文の後に続く到達不能なコード
- 宣言はされているが一度も参照されていない変数・定数
- importはされているが使われていないモジュール・パッケージ
- 条件が常にfalseになるif文のブロック
- コメントアウトされたコードの残骸（削除すべきもの）

問題とみなさないもの:
- インターフェース実装のために必要な宣言
- 将来の使用を意図したコードで、コメントで意図が明示されているもの
- テストのセットアップ・ティアダウン用コード
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するデッドコード・未使用変数に関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 明らかな未使用変数やデッドコードを見逃している
2. 誤検知: 実際には使用されているものや意図のある宣言を誤って指摘している
3. 説明の具体性: 問題箇所が具体的に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 3,
        "name": "インジェクション脆弱性",
        "category": "セキュリティ & プライバシー",
        "severity": "高",
        "review_prompt": """\
あなたはセキュリティレビューを担当するエージェントです。
以下のPR内容を確認し、SQL・コマンド・XSSなどのインジェクション脆弱性がないかチェックしてください。

問題とみなすもの:
- SQLクエリを文字列結合・フォーマットで組み立てている（プレースホルダ/パラメータバインディングを使っていない）
- ユーザー入力を含む文字列でシェルコマンドを組み立てて実行している（shell=True等）
- ユーザー入力をエスケープせずHTMLレスポンスにそのまま埋め込んでいる（XSS）
- ユーザー入力をそのままファイルパスやevalに渡している

問題とみなさないもの:
- ORMのクエリビルダやプレースホルダを使ったパラメータ化クエリ
- ユーザー入力を含まない固定文字列のみのコマンド
- サニタイズ・エスケープ処理を経由した出力
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するインジェクション脆弱性に関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際に未サニタイズの入力を使ったクエリ・コマンド・出力があるのに指摘していない
2. 誤検知: パラメータ化クエリやエスケープ済みの安全な処理を誤って指摘している
3. 説明の具体性: 問題箇所と修正方法（パラメータ化・エスケープの方法）が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 4,
        "name": "エラーハンドリングの漏れ",
        "category": "安定性 & 可用性",
        "severity": "中",
        "review_prompt": """\
あなたは安定性レビューを担当するエージェントです。
以下のPR内容を確認し、エラーハンドリングが漏れている箇所がないかチェックしてください。

問題とみなすもの:
- catch/exceptブロックで例外を握りつぶし、何もしていない（空のcatch）
- エラーを無視してログにも残さず処理を継続している
- Go言語のerr等、戻り値のエラーチェックを行わずに次の処理に進んでいる

問題とみなさないもの:
- 意図的に無視していることがコメントで明示されている
- 上位層で一括してエラーハンドリングする設計が明確な場合
- リトライ処理などで一時的な例外を握りつぶすことが妥当な場合
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するエラーハンドリングの漏れに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際にエラーを握りつぶしている・無視している箇所があるのに指摘していない
2. 誤検知: 意図的な設計や上位層でのハンドリングを誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 5,
        "name": "リソースリーク",
        "category": "安定性 & 可用性",
        "severity": "中",
        "review_prompt": """\
あなたは安定性レビューを担当するエージェントです。
以下のPR内容を確認し、ファイル・DB接続・ソケットなどのリソースリークがないかチェックしてください。

問題とみなすもの:
- ファイル・DB接続・ソケットをopen/接続した後、close処理がない
- 例外発生時にcloseされないパスがある（with/using/defer等を使っていない手動close）
- ロックを取得したまま解放されないパスがある

問題とみなさないもの:
- with構文・using・deferなどでリソース管理されている
- 呼び出し先の関数がリソースのライフサイクルを管理していることが明確な場合
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するリソースリークに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際にリソースがクローズされていない箇所があるのに指摘していない
2. 誤検知: with/using/defer等で適切に管理されているものを誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 6,
        "name": "nil / null チェックの漏れ",
        "category": "安定性 & 可用性",
        "severity": "中",
        "review_prompt": """\
あなたは安定性レビューを担当するエージェントです。
以下のPR内容を確認し、nil/nullチェックが漏れている箇所がないかチェックしてください。

問題とみなすもの:
- 外部から受け取った値（API応答・DB検索結果・Optional型）がnilの可能性があるのに、チェックせずフィールドアクセス・メソッド呼び出しをしている
- マップ/辞書からの取得結果（存在しない場合nil/None）を確認せずに使用している

問題とみなさないもの:
- 型システムやOptional/Result型によってnilになりえないことが保証されている
- 直前に存在確認・非nilチェックが行われている
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するnil/nullチェックの漏れに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際にnilチェックが漏れている箇所があるのに指摘していない
2. 誤検知: 型やコンテキスト上nilになりえない箇所を誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 7,
        "name": "入力バリデーションの漏れ",
        "category": "データ整合性 & 統合",
        "severity": "中",
        "review_prompt": """\
あなたはデータ整合性レビューを担当するエージェントです。
以下のPR内容を確認し、入力バリデーションが漏れている箇所がないかチェックしてください。

問題とみなすもの:
- 外部からの入力（APIリクエストパラメータ・フォーム入力）を型・範囲・フォーマットの検証なしに直接使用している
- 数値の範囲チェック（負数・上限超過）を行わずに計算やDB操作に使っている
- 文字列長やフォーマット（メールアドレス等）を検証せずに保存している

問題とみなさないもの:
- フレームワークやバリデーションライブラリで宣言的に検証されている
- 内部で生成された値で外部入力ではない
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対する入力バリデーションの漏れに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際に外部入力の検証が漏れている箇所があるのに指摘していない
2. 誤検知: フレームワーク等で既に検証されている箇所や内部生成値を誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 8,
        "name": "暗黙の型変換・データ型不一致",
        "category": "データ整合性 & 統合",
        "severity": "中",
        "review_prompt": """\
あなたはデータ整合性レビューを担当するエージェントです。
以下のPR内容を確認し、暗黙の型変換やデータ型の不一致がないかチェックしてください。

問題とみなすもの:
- 異なる型同士（数値と文字列など）を暗黙変換に頼って比較・演算している
- 浮動小数点数を金額計算など精度が重要な場面で使用している
- 整数のオーバーフロー・切り捨てが起こりうる型変換をしている

問題とみなさないもの:
- 意図的なキャストで、その意図がコメント等で明示されている
- 言語仕様上安全であることが明確な変換
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対する暗黙の型変換・データ型不一致に関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際に危険な暗黙変換や精度の問題があるのに指摘していない
2. 誤検知: 意図的で安全なキャストを誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 9,
        "name": "N+1 クエリ",
        "category": "パフォーマンス & スケーラビリティ",
        "severity": "中",
        "review_prompt": """\
あなたはパフォーマンスレビューを担当するエージェントです。
以下のPR内容を確認し、N+1クエリ問題がないかチェックしてください。

問題とみなすもの:
- ループの中でDBクエリやAPI呼び出しを1件ずつ実行している（一括取得・JOIN・bulkクエリを使っていない）
- N件のリストに対してループ内で関連データを都度取得している

問題とみなさないもの:
- ループ回数が明らかに小さい（定数個・数件程度）ことがコード上明らかな場合
- 事前に一括取得（eager loading、IN句によるバッチ取得）してからループしている
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するN+1クエリに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際にループ内で繰り返しクエリが発行されているのに指摘していない
2. 誤検知: 事前に一括取得済みのループや、件数が明らかに小さいものを誤って指摘している
3. 説明の具体性: 問題箇所と修正方法（一括取得の方法）が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 10,
        "name": "ループ内の重い処理",
        "category": "パフォーマンス & スケーラビリティ",
        "severity": "低",
        "review_prompt": """\
あなたはパフォーマンスレビューを担当するエージェントです。
以下のPR内容を確認し、ループ内に不要な重い処理がないかチェックしてください。

問題とみなすもの:
- ループの外に出せる計算・正規表現コンパイル・オブジェクト生成をループ内で毎回行っている
- ループ内で不変の値を毎回再計算している

問題とみなさないもの:
- ループごとに異なる入力に依存していて外に出せない処理
- パフォーマンスに影響しない程度の軽量な処理
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するループ内の重い処理に関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: 実際にループ外に出せる重い処理をループ内に書いている箇所があるのに指摘していない
2. 誤検知: ループごとに異なる入力に依存し外に出せない処理を誤って指摘している
3. 説明の具体性: 問題箇所と修正方法が明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 11,
        "name": "新規コードへのテスト漏れ",
        "category": "テスト",
        "severity": "中",
        "review_prompt": """\
あなたはテストレビューを担当するエージェントです。
以下のPR内容を確認し、新規追加・変更されたコードに対応するテストが漏れていないかチェックしてください。

問題とみなすもの:
- PRで新規追加・変更された関数やメソッドに対応するテストコードがPR内に含まれていない
- 既存テストの修正のみで、新しいロジックパスをカバーしていない

問題とみなさないもの:
- 単純な設定変更・ドキュメント修正・型定義のみの変更
- リファクタリングのみで振る舞いが変わらず、既存テストで十分カバーされている場合
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対する新規コードへのテスト漏れに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: テストが必要な新規ロジックがあるのに指摘していない
2. 誤検知: 設定変更やリファクタリングのみでテスト不要な変更を誤って指摘している
3. 説明の具体性: どの関数・ロジックにテストが必要かが明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
    {
        "id": 12,
        "name": "ガード処理に対応するテストの漏れ",
        "category": "テスト",
        "severity": "中",
        "review_prompt": """\
あなたはテストレビューを担当するエージェントです。
以下のPR内容を確認し、実装中のガード処理（nilチェック・空配列チェック・early returnなど）に
対応するテストが漏れていないかチェックしてください。

問題とみなすもの:
- 実装にnilチェック・空配列チェック・early returnなどのガード処理があるのに、それを通るテストケースがない
- if/elseの分岐が追加されているのに、片方の分岐のみテストされている

問題とみなさないもの:
- ガード処理が既存のテストで既にカバーされている
- テスト困難な外部要因（時刻・乱数など）に依存し、対応が別途必要と明示されている
""",
        "checker_prompt": """\
あなたはレビュー品質を検証するエージェントです。
以下のPR内容と、それに対するガード処理に対応するテストの漏れに関するレビュー結果を確認してください。

チェック観点:
1. 見落とし: テストされていないガード処理・分岐があるのに指摘していない
2. 誤検知: 既にテストでカバーされているガード処理を誤って指摘している
3. 説明の具体性: どのガード処理・分岐にテストが必要かが明確に示されているか

レビューが適切であれば ok=true、問題があれば ok=false とフィードバックを返してください。
""",
    },
]
