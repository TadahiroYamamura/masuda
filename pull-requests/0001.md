---
title: frontendのセットアップ
---

### 変更内容

- typescriptの導入
- eslintの導入
- main.goのroutingの修正

---

たくさん変更あるけど、このファイルだけ見てほしい

今日話した修正だと、routingをreactに任せているわけではなく、Gin側にroutingを任せていることが動かしていて分かった（/v1/page, /v1/page/profile以外のRouteをReact側に足しても動かない）

なので、[これ](https://github.com/gin-gonic/contrib/issues/90#issuecomment-990237367)を参考にして接頭が"/v1/api"以外のURLが入力されたらindex.htmlを呼び出し、React側の処理でNotFoundというcomponentを呼び出すようにした

これにより、Gin側にはルートパスだけ指定して、React側でlogin、profile、notfoundを出し分けることができるようになった

ただ、これも現状うまくいっていない
うまくいっていない点は
- http://localhost:3000/hogehoge などの全く知らないapiが来たときにgo側の処理でパスがうまく設定できていない？
- http://localhost:3000/v1/page/profile/ のようにスラッシュで区切られるとgo側の処理でパスがうまく設定出来ていない？

おそらくReact側ではなく、Go側で書いたこのファイルの処理に問題があると思うので、少しアドバイスもらえるとありがたい:pray:
